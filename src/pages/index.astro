---
import BaseLayout from '../layouts/BaseLayout.astro';
const title = 'Alex Dev - Portafolio Racing';
---
<BaseLayout title={title}>

  <div id="three-root" aria-hidden="false" role="img" aria-label="Escena 3D interactiva"></div>

  <noscript id="webgl-fallback" style="display:none;">
    <div style="position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:#050510;color:#fff;z-index:3;padding:20px;">
      <div style="text-align:center;max-width:600px;">
        <img src="/img/preview.svg" alt="Vista previa del portfolio" style="width:100%;max-width:400px;border-radius:8px;margin-bottom:20px;" />
        <p style="font-size:18px;margin:0;">Tu navegador no ejecuta JavaScript o tiene WebGL deshabilitado. Visita la sección de proyectos desde la navegación si tienes problemas.</p>
      </div>
    </div>
  </noscript>

  <div id="ui-layer" aria-live="polite" aria-atomic="true">
      <div id="info-panel">
          <h1>Alex Dev Portfolio</h1>
          <div id="speedometer">0 km/h</div>
          <p id="instructions">Controles: <strong>WASD</strong> o <strong>Flechas</strong>. Usa <kbd>Tab</kbd> para navegar a los controles táctiles y pulsa <kbd>Enter</kbd> o <kbd>Space</kbd> para activarlos; también puedes usar el ratón o tocar en móvil.</p>
      </div>

      <div id="neon-popup" role="status" aria-live="assertive">
          <h2 id="neon-title">TÍTULO</h2>
          <p id="neon-desc">Descripción</p>
      </div>

      <div id="mobile-controls">
          <div class="control-group">
              <button class="btn" id="btn-left" aria-label="Girar izquierda" title="Girar izquierda">◀</button>
              <button class="btn" id="btn-right" aria-label="Girar derecha" title="Girar derecha">▶</button>
          </div>
          <div class="control-group">
              <button class="btn btn-brake" id="btn-down" aria-label="Frenar" title="Frenar">▼</button>
              <button class="btn btn-gas" id="btn-up" aria-label="Acelerar" title="Acelerar">▲</button>
          </div>
      </div>
  </div>

  <script src="/js/three-portfolio.js" defer></script>

</BaseLayout>
